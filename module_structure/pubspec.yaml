name: module_structure
description: Conventions of Datasource, Repository and Usecase
version: 0.0.1
publish_to: "none"

environment:
  sdk: ">=3.1.2 <4.0.0"
  flutter: ">=3.7.0"

dependencies:
  flutter:
    sdk: flutter
  fpdart: ^0.6.0
  rxdart: ^0.27.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  coolint: 1.0.0

flutter:
  generate: true
